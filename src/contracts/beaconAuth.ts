export interface BeaconAuthResult {
  result: string;
  cpid: string;
  publicKey: string;
  verificationCode: string;
}

export interface AdvertiseBeaconV3Result {
  result: string;
  cpid: string;
  publicKey: string;
  verificationCode: string;
}
